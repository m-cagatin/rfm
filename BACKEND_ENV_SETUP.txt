================================================================================
BACKEND .env FILE SETUP - COPY THIS EXACTLY
================================================================================

YOU NEED TO CREATE THIS FILE: backend/.env

Copy everything below the line into that file:

--------------------------------------------------------------------------------
# Database Configuration (Aiven Cloud MySQL)
DB_HOST=rfmdb-euniquecorn.d.aivencloud.com
DB_PORT=28152
DB_USER=Marcc
DB_PASSWORD=Marcc1234
DB_NAME=rfm_db

# JWT Secret (REQUIRED FOR LOGIN TO WORK)
JWT_SECRET=rfm-super-secret-jwt-key-2024-change-this-in-production
JWT_EXPIRATION=24h

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:4200
--------------------------------------------------------------------------------

SAVE THIS AS: backend/.env

THEN RESTART YOUR BACKEND SERVER.

Your admin login will work after this:
- Email: admin@rfm.com
- Password: (whatever password you set for that account)

================================================================================

